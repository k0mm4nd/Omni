package ddos

import (
	// "github.com/k0mm4nd/Omni/modules/ddos"
	"testing"
	// "time"
)

func Test_ddos(t *testing.T) {
}

func Run_ddos() {
	// t.Log("...")
	// workers := 100
	// d, err := ddos.New("http://pasc.cc:80", workers)
	// if err != nil {
	// 	panic(err)
	// }
	// d.Run()
	// time.Sleep(time.Second)
	// d.Stop()
}
